name: offline_first_sync_drift_rest
description: REST transport adapter for offline_first_sync_drift.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.7.0

dependencies:
  offline_first_sync_drift:
    path: ../offline_first_sync_drift
  http: ^1.4.0

dev_dependencies:
  test: ^1.25.15
  drift: ^2.26.0
  drift_dev: ^2.26.0
  build_runner: ^2.4.15
